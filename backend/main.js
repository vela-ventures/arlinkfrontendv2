import { runBuild } from "./buildManager.js";

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error);

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You can keep adding more builds, they will be queued and run as slots become available

// First build
runBuild(
  "https://github.com/internettrashh/preact-test-template.git",
  "main",
  "bun install",
  "bun run build",
  "dist",
)
  .then(console.log)
  .catch(console.error); // You
